<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>큐마켓 관리자</title>
    <style>
        * {
            box-sizing: border-box; 
            font-family: sans-serif;
        }

        html {
            height: 100%;
        }

        body {
            height: 100%;
            display: flex;
            flex-direction: column;
            margin: 0;
        }

        body>article {
            width: 100%;
            height: 100%;
        }

        iframe {
            width: 100%;
            height: 100%;
            border: none;
        }

        .pc {
            display: none;
            font-size: 0.4em;
            font-weight: normal;
            color: red;
        }

        @media screen and (max-width: 768px) {
            * {
                font-size: 0.95em;
            }

            .pc {
                display: inline;
            }
        }
    </style>
</head>

<body>
    <div style="display: flex; flex-direction: row; justify-content: space-between;">
        <h1>상품 재고관리<span class="pc">*PC에 최적화 되어있습니다.</span></h1>
        <div style="align-self: center;">
            <select style="width:200px; text-align-last: center;" id="parent_category" onchange="child_category_change(this)">
                <option value="0">전체</option>
                <option value="1">신선식품</option>
                <option value="2">가정 간편식</option>
                <option value="3">생활용품</option>
                <option value="4">기타</option>
                <option value="5">우리동네가게</option>
            </select>
            <select style="width:200px; text-align-last: center;" id="child_category" onchange="grand_child_category_change(this)">
                <option value="0">전체</option>
            </select>
            <select style="width:200px; text-align-last: center;" id="grand_child_category">
                <option value="0">전체</option>
            </select>
            <input id="search" type="text">
            <input type="button" value="검색" onclick="products_search('<%= data.partner_idx %>', this)"></button>
        </div>
    </div>
    <article>
        <iframe id="content" src="/manager/<%= data.partner_idx %>/products?page=1"></iframe>  
    </article>
    <script src="/javascripts/myScript.js"></script>
</body>

</html>